# v0.2 update

This update changes the homepage wording, adds a project gallery, expands selected publications, and adds publication image placeholders.

Apply in your local repository, then run:

```bash
quarto render
git add .
git commit -m "Update homepage text and publication layout"
git push
```

GitHub Pages should remain configured as:

```text
Source: Deploy from a branch
Branch: main
Folder: /docs
```
